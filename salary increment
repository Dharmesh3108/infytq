def find_new_salary(current_salary,job_level):
    # write your logic here
    if(job_level==3):
        current_salary=current_salary+(current_salary*(15/100))
        new_salary=current_salary
    elif(job_level==4):
        current_salary=current_salary+(current_salary*(7/100))
        new_salary=current_salary    
    elif(job_level==5):
        current_salary=current_salary+(current_salary*(5/100))
        new_salary=current_salary
    else:
        new_salary=current_salary
    
    return new_salary
   

new_salary=find_new_salary(10000,3)
print(new_salary)
